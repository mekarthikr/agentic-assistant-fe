VITE_WS_URL=wss://agentic-assistant-be.vercel.app/ws
# Set this only when the backend SOCKET_AUTH_TOKEN is configured.
VITE_WS_AUTH_TOKEN=token
VITE_API_URL=https://agentic-assistant-be.vercel.app
# POC only: VITE_ values are browser-visible. Replace with real admin auth for production.
VITE_RAG_INDEX_TOKEN=match-backend-rag-index-token
# The Agent/Client toggle selects the hardcoded application profile.
